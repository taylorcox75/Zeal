<%= form_for(reminder) do |f| %>
  <% if reminder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reminder.errors.count, "error") %> prohibited this reminder from being saved:</h2>

      <ul>
      <% reminder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query %>
    <%= f.text_area :query %>
  </div>

  <div class="field">
    <%= f.label :facebook_user_id %>
    <%= f.text_field :facebook_user_id %>
  </div>

  <div class="field">
    <%= f.label :dueDate %>
    <%= f.datetime_select :dueDate %>
  </div>

  <div class="field">
    <%= f.label :priority %>
    <%= f.number_field :priority %>
  </div>

  <div class="field">
    <%= f.label :done %>
    <%= f.check_box :done %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
